{
  "name": "zugferd-rechnung",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "sample": "tsx scripts/sample.ts",
    "validate": "scripts/validate.sh",
    "check": "npm test && npm run build && npm run sample && npm run validate"
  },
  "dependencies": {
    "@pdf-lib/fontkit": "^1.1.1",
    "pdf-lib": "^1.17.1"
  },
  "devDependencies": {
    "@types/node": "^26.5.0",
    "tsx": "^4.23.13",
    "typescript": "^7.0.2",
    "vite": "^8.2.2",
    "vitest": "^5.0.0"
  },
  "description": "ZUGFeRD/Factur-X-Rechnungen (EN 16931, PDF/A-3) im Browser erzeugen – mit GLN-Feldern für Markant",
  "license": "MIT",
  "engines": {
    "node": ">=20"
  }
}
